---
language: java

before_install:
  - echo this is the
  - echo before_install option

script:
  - echo attempting to build
  - echo "here it goes..."
  - ls -lthr
  - ./common-tools/build.sh -m

jdk:
- oraclejdk8
