---
language: java

jdk:
- oraclejdk8

before_install:
  - ls -lthra
  - java -version
  - echo "starting build..."

install:
  - ./build-coatjava.sh

script:
  - ls -lthra
  - which groovy

notifications:
  emails: false
