---
language: java

before_install:
  - echo this is the
  - echo before_install option

script:
  - echo attempting to build
  - echo "here it goes..."
  - ls -lthr
  - ./build-coatjava.sh
  - ls -lthr

jdk:
- oraclejdk8

notifications:
  emails: false
