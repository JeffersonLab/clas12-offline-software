---
language: java

before_install:
  - echo this is the
  - echo before_install option

script:
  - echo attempting to build
  - echo "here it goes..."
  - ls -lthr
  - cd common-tools
  - lst -lthr
  - ./build.sh -m

jdk:
- oraclejdk8
